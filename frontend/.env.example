# PattyShack Frontend Environment Variables

# API URL for backend server
# Ensure this value is also configured in hosting dashboards such as
# Vercel or Railway when deploying the frontend.
VITE_API_URL=http://localhost:3000/api/v1
VITE_DEV_MODE=true

# Production URL (uncomment when deploying)
# VITE_API_URL=https://your-api-domain.com/api/v1
